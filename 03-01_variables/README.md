# Variables and mutability

Explores immutability by default, the `mut` keyword and variable shadowing. Shadowing lets a
value be transformed a few times, or even change type, while reusing the same name. Unlike
`mut`, it still catches accidental reassignment at compile time.

Docs: https://doc.rust-lang.org/book/ch03-01-variables-and-mutability.html
